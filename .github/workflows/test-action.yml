name: Test Action

on: [push]

jobs:
  build-linux:
    runs-on: node16
    steps:
    - name: Git Quality Check
      uses: gcattan/git-quality-check@v0.1
      with:
        bad_words: 'WIP'
        main_branches: 'origin/master'
        
